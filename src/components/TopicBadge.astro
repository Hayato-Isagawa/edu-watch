---
interface Props {
  topic: string;
}
const { topic } = Astro.props;
---

<span
  class="inline-flex items-center px-2 py-0.5 text-[11px] font-medium tracking-wide text-sub border border-line rounded-sm whitespace-nowrap bg-card"
>
  {topic}
</span>
